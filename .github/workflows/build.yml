name: Test and build

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    strategy:
      matrix:
        # macOS stands in for the Pi during development, Windows is the
        # Companion's real target, and Linux is the closest runner to the Pi.
        os: [macos-latest, windows-latest, ubuntu-latest]
        python: ["3.9", "3.13"]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - run: python -m pip install --upgrade pip
      # Easy Connect is deliberately dependency-free: the Pi install needs no
      # pip packages at all. pytest is the only build-time addition.
      - run: python -m pip install . pytest
      - run: python -m pytest -q

  build-windows:
    needs: test
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - run: python -m pip install . pyinstaller
      - run: python -m PyInstaller packaging/PiTracCompanion.spec
      - uses: actions/upload-artifact@v4
        with:
          name: PiTracCompanion-windows-x64
          path: dist/PiTracCompanion.exe
          if-no-files-found: error

